# CodeLib
CodeLib is a centralized, organized library where developers can save, tag and store reusable blocks of code. Avoiding searching old projects for specific code, but instead retrieving, copying and inserting code instantly from a digital vault.
